# Bank Tenant Onboarding Contract

This document defines the minimum contract for adding a new bank tenant runner.

## 1) Runner Implementation Contract

Implement `ILegacyBankRunner` from `platform.bank.interfaces`:

- Method: `run(self, request: BankRunRequest) -> None`
- Responsibility: execute tenant-specific legacy ETL and create output workbook at `request.output_path`
- Must raise exceptions on failure (do not silently swallow errors)

Recommended pattern:

- Put tenant adapter in `platform/bank/<tenant>_legacy.py`
- Reuse shared helpers from `platform/bank/legacy_common.py`
  - `stdlib_platform_guard`
  - `load_legacy_module`
  - `resolve_sqlite_output_path`

## 2) Registration

Register runner in `BankService` using one of:

- Static defaults in `_DEFAULT_LEGACY_BANK_RUNNERS`
- Runtime registration via:
  - `BankService.register_legacy_runner("new_tenant", NewTenantLegacyBankRunner())`

Tenant ids are normalized to lowercase.

## 3) Output Artifacts

Required:

- Workbook at `request.output_path` with parseable transaction sheet

Generated by `BankService`:

- Canonical JSON: `<output>.processed.json`
- Optional diagnostics: `<output>.parse_diagnostics.json` on parse failures

For GUI/API flows, prefer calling `BankService.run_with_result(...)` to get a `BankPipelineResult`.

## 4) Error Handling Rules

- Raise explicit exceptions when mandatory input is missing
- Convert unexpected legacy exits to runtime errors where needed
- Ensure output workbook existence is validated before returning success

## 5) Minimal Smoke Test Checklist

- Tenant run succeeds with known sample input
- Workbook output is created
- `processed.json` artifact is created
- Parsed row count > 0
